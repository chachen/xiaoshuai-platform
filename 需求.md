创建需求说明：

## 1. 项目目标

建设一套可通过 Maven 依赖接入现有 Spring Boot 项目的通用平台底座。
业务项目最终只需引入：

```xml
<dependency>
    <groupId>io.github.chachen.platform</groupId>
    <artifactId>xs-platform-starter</artifactId>
    <version>0.1.0-SNAPSHOT</version>
</dependency>

业务项目继续关注自身业务领域，登录、权限、用户管理、验证码、字典、日志、文件、脱敏等能力由底座统一提供。

平台主体不作为独立服务部署。xs-platform-test-app 仅用于集成测试。

2. 技术基线

* Java 21
* Spring Boot 3.5.x
* Maven 多模块
* Jakarta API
* 默认支持 Servlet Web
* 基础包名：io.github.chachen.platform
* 当前版本：0.1.0-SNAPSHOT

3. 参考项目

本项目参考 RuoYi-Vue-Plus 的功能设计、模块划分和部分成熟实现。

RuoYi-Vue-Plus 参考仓库仅用于源码阅读，不作为本项目 Maven 依赖，也不将其整体删除模块后打包成 Jar。

参考原则：

1. 统一响应、异常、错误码、配置属性等基础规范由本项目实现。
2. 验证码、日志、文件、脱敏等成熟实现可以选择性迁移。
3. 迁移代码时必须移除 RuoYi-Vue-Plus 专属工具类、实体类和模块耦合。
4. 用户、角色、权限、认证和字典参考其业务流程和数据模型，按本项目模块边界重新组织。
5. 每次只迁移一个能力。
6. 使用第三方源码时检查许可证，并保留必要的来源、版权和许可证说明。

4. 当前进度

当前模块：

xiaoshuai-platform
├── xs-platform-core
├── xs-platform-web
├── xs-platform-autoconfigure
├── xs-platform-starter
└── xs-platform-test-app

当前已完成：

* Maven 多模块工程
* Spring Boot 自动配置机制
* xs-platform-starter 聚合入口
* ApiResult<T> 统一响应
* ErrorCode
* CommonErrorCode
* BusinessException
* GlobalExceptionHandler
* 测试应用
* 六模块 Maven Reactor 构建成功
* Jar 可以通过 mvn clean install 安装到本地 Maven 仓库

测试应用出现 8080 端口占用不影响平台构建。测试时使用随机端口：

server:
  port: 0

5. 最终功能范围

5.1 Web 基础能力

* 统一响应
* 统一错误码
* 全局异常处理
* 参数校验异常
* JSON 解析异常
* 请求方法异常
* TraceId
* MDC 日志上下文
* 配置开关 xs.web.enabled

5.2 验证码

* 图片验证码生成
* 验证码存储
* 验证码校验
* TTL
* 一次性消费
* 大小写配置
* 内存存储默认实现
* Redis 存储扩展
* 配置开关 xs.captcha.enabled

建议接口：

public interface CaptchaService {
    CaptchaResult generate();
    void verify(String captchaKey, String answer);
}
public interface CaptchaStore {
    void save(String key, String value, Duration ttl);
    String get(String key);
    void delete(String key);
}

5.3 数据库基础能力

* MyBatis-Plus 基础配置
* 分页
* 字段自动填充
* 乐观锁
* 防止全表更新删除
* Flyway 数据库迁移
* 配置开关 xs.database.enabled

5.4 用户、角色和权限

* 用户管理
* 角色管理
* 菜单管理
* 权限管理
* 用户角色关系
* 角色权限关系
* 用户启用、禁用、锁定
* BCrypt 密码哈希
* 默认管理员初始化

基础表：

sys_user
sys_role
sys_user_role
sys_permission
sys_role_permission
sys_menu

数据库实体不得直接作为 API 返回对象。

5.5 登录认证

* 登录
* 验证码校验
* Token 签发
* Token 刷新
* 注销
* 当前用户上下文
* 接口权限校验
* 白名单路径
* 登录失败次数控制
* 登录日志
* 配置开关 xs.auth.enabled

认证模块通过接口访问用户信息，不能直接依赖系统模块数据库实体。

5.6 字典

* 字典类型
* 字典项
* 启用和停用
* 排序
* 查询接口
* 本地缓存
* Redis 缓存扩展
* 缓存刷新
* 配置开关 xs.dict.enabled

5.7 日志

* 操作日志
* 登录日志
* 异常日志
* @OperationLog
* 请求路径
* HTTP 方法
* 用户
* IP
* TraceId
* 执行耗时
* 请求参数截断
* 响应结果截断
* 敏感字段过滤
* 异步写入
* 配置开关 xs.log.enabled

5.8 文件管理

* FileStorage 抽象
* 本地存储默认实现
* MinIO 或 S3 扩展
* 文件元数据
* 上传
* 下载
* 删除
* 临时访问地址
* 文件大小校验
* 扩展名校验
* MIME 校验
* 路径穿越防护
* 配置开关 xs.file.enabled

业务表保存文件 ID 或 file key，不保存服务器绝对路径。

5.9 数据脱敏

* @Desensitize
* 手机号
* 身份证号
* 银行卡
* 邮箱
* 姓名
* 地址
* Jackson 序列化脱敏
* 日志脱敏
* 自定义策略
* 按权限决定是否显示原文
* 配置开关 xs.desensitize.enabled

6. 计划模块

xs-platform-captcha
xs-platform-database
xs-platform-system
xs-platform-auth
xs-platform-dict
xs-platform-log
xs-platform-file
xs-platform-desensitize
xs-platform-test-support

7. 模块边界

xs-platform-core

仅存放低耦合模型、接口、异常和上下文。

禁止依赖：

* Spring MVC
* MyBatis
* 数据库实体
* Redis 实现
* 文件存储实现

xs-platform-autoconfigure

存放：

* 配置属性
* 自动配置类
* 条件装配
* 默认 Bean 注册

默认实现使用：

@ConditionalOnMissingBean

所有功能必须支持独立开关。

xs-platform-starter

业务项目唯一依赖入口，只负责聚合模块。

xs-platform-test-app

仅用于测试，不允许被任何公共模块依赖。

8. 配置规范

统一使用 xs 前缀：

xs:
  platform:
    enabled: true
  web:
    enabled: true
  captcha:
    enabled: true
  database:
    enabled: true
  system:
    enabled: true
  auth:
    enabled: true
  dict:
    enabled: true
  log:
    enabled: true
  file:
    enabled: true
  desensitize:
    enabled: true

9. 开发顺序

1. 完善 Web 基础能力
2. 验证码
3. 数据库基础能力
4. 用户、角色、权限
5. 登录认证
6. 字典
7. 日志
8. 文件
9. 数据脱敏
10. 发布到私有 Maven 仓库

每个阶段完成后必须先通过测试，再开始下一阶段。

10. 第一版验收标准

第一版完成后，新 Spring Boot 项目应满足：

* 只引入 xs-platform-starter
* 配置数据源和 Redis 后可以启动
* Flyway 自动初始化平台表
* 可以获取验证码
* 可以登录和注销
* 可以管理用户、角色和权限
* 可以保护业务接口
* 可以查询字典
* 可以记录登录和操作日志
* 可以上传、下载和删除文件
* API 敏感字段可以脱敏
* 各模块可以独立启停
* 业务项目可以覆盖默认实现
* mvn clean verify 全部成功

11. 非目标

第一版暂不实现：

* 微服务
* 网关
* 多租户
* 工作流
* 消息队列
* 分布式事务
* 注册中心
* 配置中心
* 低代码平台
    EOF

再创建 Codex 规则文件：
```bash
cat > AGENTS.md <<'EOF'
# Codex 开发规则
## 项目目标
本项目是面向 Spring Boot 业务项目的 Maven 平台底座。
开始开发前必须阅读：
- `README.md`
- `docs/PLATFORM_REQUIREMENTS.md`
- 根 `pom.xml`
- 相关模块的 `pom.xml`
- 相关模块现有源码
RuoYi-Vue-Plus 参考仓库用于查看成熟实现。不得将其整体复制、整体打包或直接作为本项目依赖。
## 固定约束
不得修改：
```text
groupId: io.github.chachen.platform
version: 0.1.0-SNAPSHOT
base package: io.github.chachen.platform

技术基线：

Java 21
Spring Boot 3.5.x
Maven
Jakarta API

开发规则

1. 每次只实现一个明确能力。
2. 修改前先分析当前依赖关系。
3. 新增模块必须同时加入根 pom.xml。
4. 需要被业务项目使用的模块必须加入 xs-platform-starter/pom.xml。
5. 自动配置注册到：

META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports

6. 自动配置必须有配置开关。
7. 默认 Bean 使用 @ConditionalOnMissingBean。
8. 不得覆盖业务项目已有同类型 Bean。
9. Controller 只负责协议转换和参数校验。
10. API 返回使用 ApiResult<T>。
11. 业务错误使用 BusinessException 和明确错误码。
12. 不向客户端返回堆栈、SQL、绝对路径或内部异常。
13. 密码、Token、身份证号、手机号不得写入普通日志。
14. xs-platform-core 不得依赖 Spring MVC、MyBatis 或具体存储实现。
15. 公共模块不得依赖 xs-platform-test-app。
16. 数据库变更必须使用 Flyway。
17. 数据库实体不得直接作为 API 返回对象。
18. 每个能力必须有单元测试或集成测试。
19. 每个任务完成后更新 README 当前状态或需求文档。
20. 每个任务完成后执行：

mvn clean verify

Reactor 所有模块必须为 SUCCESS。

RuoYi-Vue-Plus 参考规则

允许：

* 阅读功能实现
* 参考数据结构
* 参考权限流程
* 迁移独立且成熟的实现
* 改造验证码、日志、文件和脱敏代码

禁止：

* 整体复制模块
* 保留若依专属包名
* 将若依工具类体系完整搬入
* 让公共模块依赖若依实体
* 将若依启动工程打成 Starter
* 未检查许可证就复制源码

迁移代码前必须列出：

1. 原代码位置
2. 原代码依赖
3. 需要保留的逻辑
4. 需要移除的若依耦合
5. 新模块位置
6. 测试方案

当前下一任务

完善 xs-platform-web：

* 为 ApiResult 增加 traceId
* 增加 TraceId Filter
* TraceId 写入 MDC
* TraceId 写入响应头
* 完善参数绑定异常
* 完善 JSON 反序列化异常
* 完善请求方法异常
* 增加 xs.web.enabled
* 增加自动配置测试
* 增加 MockMvc 集成测试
* 测试应用使用随机端口

完成后执行 mvn clean verify，并汇报修改文件、测试结果和剩余风险。
EOF

README 保持简洁：
```bash
cat > README.md <<'EOF'
# Xiaoshuai Platform
面向 Spring Boot 业务项目的通用 Maven 平台底座。
业务项目通过 Starter 复用统一响应、异常处理、认证、权限、验证码、用户管理、字典、日志、文件和数据脱敏等能力。
## 当前状态
已完成：
- Maven 多模块工程
- Spring Boot 自动配置
- `xs-platform-core`
- `xs-platform-web`
- `xs-platform-autoconfigure`
- `xs-platform-starter`
- `xs-platform-test-app`
- 统一响应
- 全局异常处理
## 构建
```bash
mvn clean install

业务项目引用

<dependency>
    <groupId>io.github.chachen.platform</groupId>
    <artifactId>xs-platform-starter</artifactId>
    <version>0.1.0-SNAPSHOT</version>
</dependency>

文档

* docs/PLATFORM_REQUIREMENTS.md：完整需求和验收标准
* AGENTS.md：Codex 开发规则

参考项目

本项目参考 RuoYi-Vue-Plus 的功能设计和部分成熟实现，但不直接依赖或整体打包 RuoYi-Vue-Plus。
EOF

## 给 Codex 的启动提示词
在 `xiaoshuai-platform` 项目中打开 Codex，直接发送：
```text
请继续开发当前 Xiaoshuai Platform 项目。
开始前必须完整阅读：
1. README.md
2. AGENTS.md
3. docs/PLATFORM_REQUIREMENTS.md
4. 根 pom.xml
5. 当前全部模块 pom.xml
6. xs-platform-web 和 xs-platform-autoconfigure 的现有源码
本项目参考 RuoYi-Vue-Plus 的功能设计和部分成熟实现。RuoYi-Vue-Plus 参考仓库位于当前工作区旁边，仅用于查看实现。禁止整体复制、整体删除模块后打包，禁止直接依赖 RuoYi-Vue-Plus。
当前只完成 AGENTS.md 中的“当前下一任务”，即完善 xs-platform-web：
1. ApiResult 增加 traceId。
2. 实现 TraceId Filter。
3. TraceId 写入 MDC。
4. TraceId 写入响应头。
5. 完善参数绑定异常处理。
6. 完善 JSON 反序列化异常处理。
7. 完善 HTTP 请求方法异常处理。
8. 增加 xs.web.enabled 配置属性。
9. 默认 Bean 使用 ConditionalOnMissingBean。
10. 增加自动配置测试。
11. 增加 MockMvc 集成测试。
12. 将测试应用端口改为随机端口。
要求：
1. 不修改 groupId、版本号和基础包名。
2. 不实现验证码或后续模块。
3. 不做无关重构。
4. 不向客户端暴露内部异常信息。
5. 完成后执行 mvn clean verify。
6. Reactor 全部 SUCCESS 才算完成。
7. 最后汇报修改文件、设计说明、测试结果和剩余风险。

最后检查并提交：

git status
git add README.md AGENTS.md docs/PLATFORM_REQUIREMENTS.md
git commit -m "docs: add platform requirements and codex rules"
git push

## 2026-08 实现状态

已落地并可由 xs-platform-starter 自动装配的能力：

* 验证码内存实现，以及检测到 StringRedisTemplate 后的 Redis 实现。
* 本地文件实现，以及配置 xs.file.backend=minio 后的 MinIO 实现。
* 用户、角色、权限、用户角色、角色权限和菜单基础管理接口。
* JWT 认证、刷新、注销、当前用户、登录失败锁定和 RequirePermission。
* Flyway V9001/V9002：平台系统表、字典表、操作日志表、登录日志表；迁移脚本使用独立路径和标准 SQL，支持 MySQL 和 PostgreSQL。
* 平台系统表统一使用 `xs_` 前缀，单表主键使用 MyBatis-Plus `ASSIGN_ID`，不依赖数据库自增。
* 统一响应返回数字 `code`、`requestId`，并支持关闭平台全局 SecurityFilterChain 以接入已有安全体系。
* 平台提供可直接执行的 `sql/xs-platform.sql`；平台不会自动接管宿主项目的 Flyway locations。
* 操作日志切面、数据库异步写入和登录事件数据库写入。
* H2 测试应用集成启动验证；生产环境可使用 MySQL 或 PostgreSQL，并由业务项目提供数据库驱动。

当前仍保留为后续增强项：

* Redis 限流、分布式登录失败计数和分布式会话撤销。
* 部门/组织树和数据权限。
* 短信、邮箱验证码和在线用户管理。
* S3 兼容客户端、文件临时签名 URL 和文件元数据表。
* SQL Server、Oracle 等其他数据库方言脚本和更完整的单元/集成测试。
